{
  "watch": ["server"],
  "ext": "ts,js,json",
  "ignore": ["dist", "node_modules", ".next"],
  "exec": "ts-node --project tsconfig.server.json --transpile-only server/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
